---
---

@import "{{ site.theme }}";

// STYLING PREFERNCES for SITE
// mixin used as a template for buttons
  @mixin button {
    width: auto;
    height: auto;
    border-radius: 10px;
    background-color: #21807c;
    border: 3px solid rgb(0, 0, 0);
    font-size: 1.5em;

    display: flex;
    justify-content: center;
    align-items: center;

    grid-column: span 1;
    grid-row: span 1;

    // Creates smooth animation effect
    transition: all 0.5s; 
  }

  /* define class for redifined button */
  .button {
    @include button;
  }

   /* darkens the background color on hover to create a selecting effect */
  .button:hover {
    background-color: #373737;
  }

  /* "row style" is flexible size and aligns pictures in center */
  .row {
    align-items: center;
    display: flex;
  }

  /* "column style" is one-third of the width with padding */
  .column {
    flex: 16.66%;
    padding: 3px;
  }
  
// STYLING FOR CALCULATOR
  /* class to create the calculator's container; uses CSS grid dsiplay to partition off buttons */
  .calculator-container { 
    width: 90vw; /* this width and height is specified for mobile devices by default */
    height: 80vh;
    margin: 0 auto;
  
    display: grid;
    grid-template-columns: repeat(4, 1fr); /* fr is a special unit; learn more here: https://css-tricks.com/introduction-fr-css-unit/  */
    grid-template-rows: 0.5fr repeat(4, 1fr);
    gap: 10px 10px;
  }
  
  @media (min-width: 600px) { 
    .calculator-container {
        width: 40vw;
        height: 80vh;
    }
  }

  /* styling for the calculator number button */
  .calculator-number {
    @extend .button;
  }

  /* styling for the calculator operation button */
  .calculator-operation {
    @extend .button;
  }
  
  /* styling for the calculator clear button */
  .calculator-clear {
    @extend .button;
    background-color: #e68b1c;
  }
  
  /* styling for the calculator equals button */
  .calculator-equals {
    @extend .button;
    background-color: #490fe7;
  }

  body {
    font-family: Arial, sans-serif;
    text-align: center;
}

.quiz-container {
    max-width: 400px;
    margin: 0 auto;
}

.options button {
    margin: 5px;
    padding: 10px 20px;
    font-size: 16px;
}

.next-button {
    background-color: #4CAF50;
    color: white;
    border: none;
    cursor: pointer;
}

.next-button:hover {
    background-color: #45a049;
}

/* Base styles */
body {
  background-color: #ffe0f0; /* Light pink background */
  font-family: Arial, sans-serif;
  color: #ff1493; /* Deep pink text color */
}

/* Header styles */
.header {
  background-color: #ff69b4; /* Hot pink header background */
  color: white; /* White text color for header */
  padding: 20px;
  text-align: center;
}

/* Button styles */
.button {
  background-color: #ff1493; /* Deep pink button background */
  color: white; /* White text color for buttons */
  padding: 10px 20px;
  border: none;
  border-radius: 5px;
  cursor: pointer;
}

.button:hover {
  background-color: #ff69b4; /* Hover color */
}

/* Links */
a {
  color: #ff69b4; /* Pink links */
  text-decoration: none;
}

a:hover {
  text-decoration: underline; /* Underline on hover */
}
/* Base styles */
body {
  background: linear-gradient(to bottom, #ff6b6b, #ff1b1b); /* Pink sunset gradient background */
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  color: #ffcccb; /* Cream text color */
  margin: 0;
  padding: 0;
}

/* Header styles */
.header {
  background-color: #ffcccb; /* Cream header background */
  color: #ff6b6b; /* Pink text color for header */
  padding: 20px;
  text-align: center;
  box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.3); /* Shadow for header */
}

/* Button styles */
.button {
  background-color: #ff6b6b; /* Pink button background */
  color: white; /* White text color for buttons */
  padding: 10px 20px;
  border: none;
  border-radius: 5px;
  cursor: pointer;
  font-weight: bold;
  transition: background-color 0.3s; /* Smooth hover effect */
}

.button:hover {
  background-color: #ff1b1b; /* Darker pink on hover */
}

/* Links */
a {
  color: #ff6b6b; /* Pink links */
  text-decoration: none;
  font-weight: bold;
  transition: color 0.3s; /* Smooth color change on hover */
}

a:hover {
  color: #ff1b1b; /* Darker pink on hover */
}

/* Sunset Image */
.sunset {
  max-width: 100%;
  height: auto;
  display: block;
  margin: 20px auto;
  box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.3); /* Shadow for image */
  border-radius: 10px;
}

/* Content container */
.container {
  background-color: rgba(255, 255, 255, 0.9); /* Semi-transparent white container background */
  padding: 20px;
  border-radius: 10px;
  box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.3); /* Shadow for container */
  margin: 20px auto;
  max-width: 800px;
}

/* Text styles */
h1, h2, h3 {
  color: #ff6b6b; /* Pink headers */
}

p {
  line-height: 1.6;
}

/* Footer styles */
.footer {
  background-color: #ffcccb; /* Cream footer background */
  color: #ff6b6b; /* Pink text color for footer */
  padding: 10px 0;
  text-align: center;
}
/* Base styles */
body {
  background: linear-gradient(to bottom, #ff6b6b, #ff1b1b); /* Pink sunset gradient background */
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  color: #ffcccb; /* Cream text color */
  margin: 0;
  padding: 0;
}

/* Header styles */
.header {
  background-color: #ffcccb; /* Cream header background */
  color: #ff6b6b; /* Pink text color for header */
  padding: 20px;
  text-align: center;
  box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.3); /* Shadow for header */
}

/* Button styles */
.button {
  background-color: #ff6b6b; /* Pink button background */
  color: white; /* White text color for buttons */
  padding: 10px 20px;
  border: none;
  border-radius: 5px;
  cursor: pointer;
  font-weight: bold;
  transition: background-color 0.3s; /* Smooth hover effect */
}

.button:hover {
  background-color: #ff1b1b; /* Darker pink on hover */
}

/* Links */
a {
  color: #ff6b6b; /* Pink links */
  text-decoration: none;
  font-weight: bold;
  transition: color 0.3s; /* Smooth color change on hover */
}

a:hover {
  color: #ff1b1b; /* Darker pink on hover */
}

/* Sunset Image */
.sunset {
  max-width: 100%;
  height: auto;
  display: block;
  margin: 20px auto;
  box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.3); /* Shadow for image */
  border-radius: 10px;
}

/* Content container */
.container {
  background-color: rgba(255, 255, 255, 0.9); /* Semi-transparent white container background */
  padding: 20px;
  border-radius: 10px;
  box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.3); /* Shadow for container */
  margin: 20px auto;
  max-width: 800px;
}

/* Text styles */
h1, h2, h3 {
  color: #ff6b6b; /* Pink headers */
}

p {
  line-height: 1.6;
}

/* Footer styles */
.footer {
  background-color: #ffcccb; /* Cream footer background */
  color: #ff6b6b; /* Pink text color for footer */
  padding: 10px 0;
  text-align: center;
}
/* Base styles */
body {
  background: linear-gradient(to bottom, #ffb6c1, #ffc0cb); /* Light pink sunset gradient background */
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  color: #ffcccc; /* Light pink text color */
  margin: 0;
  padding: 0;
}

/* Header styles */
.header {
  background-color: #ffcccc; /* Light pink header background */
  color: #ff69b4; /* Darker pink text color for header */
  padding: 20px;
  text-align: center;
  box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.3); /* Shadow for header */
}

/* Button styles */
.button {
  background-color: #ff69b4; /* Darker pink button background */
  color: white; /* White text color for buttons */
  padding: 10px 20px;
  border: none;
  border-radius: 5px;
  cursor: pointer;
  font-weight: bold;
  transition: background-color 0.3s; /* Smooth hover effect */
}

.button:hover {
  background-color: #ff1b1b; /* Darker pink on hover */
}

/* Links */
a {
  color: #ff69b4; /* Darker pink links */
  text-decoration: none;
  font-weight: bold;
  transition: color 0.3s; /* Smooth color change on hover */
}

a:hover {
  color: #ff1b1b; /* Darker pink on hover */
}

/* Sunset Image */
.sunset {
  max-width: 100%;
  height: auto;
  display: block;
  margin: 20px auto;
  box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.3); /* Shadow for image */
  border-radius: 10px;
}

/* Content container */
.container {
  background-color: rgba(255, 255, 255, 0.9); /* Semi-transparent white container background */
  padding: 20px;
  border-radius: 10px;
  box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.3); /* Shadow for container */
  margin: 20px auto;
  max-width: 800px;
}

/* Text styles */
h1, h2, h3 {
  color: #ff69b4; /* Darker pink headers */
}

p {
  line-height: 1.6;
}

/* Footer styles */
.footer {
  background-color: #ffcccc; /* Light pink footer background */
  color: #ff69b4; /* Darker pink text color for footer */
  padding: 10px 0;
  text-align: center;
}
/* Base styles */
body {
  background: linear-gradient(to bottom, #ffb6c1, #ffc0cb); /* Light pink sunset gradient background */
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  color: #000; /* Black text color */
  margin: 0;
  padding: 0;
}

/* Header styles */
.header {
  background-color: #ffcccc; /* Light pink header background */
  color: #000; /* Black text color for header */
  padding: 20px;
  text-align: center;
  box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.3); /* Shadow for header */
}

/* Button styles */
.button {
  background-color: #ff69b4; /* Darker pink button background */
  color: #fff; /* White text color for buttons */
  padding: 10px 20px;
  border: none;
  border-radius: 5px;
  cursor: pointer;
  font-weight: bold;
  transition: background-color 0.3s; /* Smooth hover effect */
}

.button:hover {
  background-color: #ff1b1b; /* Darker pink on hover */
}

/* Links */
a {
  color: #ff69b4; /* Darker pink links */
  text-decoration: none;
  font-weight: bold;
  transition: color 0.3s; /* Smooth color change on hover */
}

a:hover {
  color: #ff1b1b; /* Darker pink on hover */
}

/* Sunset Image */
.sunset {
  max-width: 100%;
  height: auto;
  display: block;
  margin: 20px auto;
  box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.3); /* Shadow for image */
  border-radius: 10px;
}

/* Content container */
.container {
  background-color: rgba(255, 255, 255, 0.9); /* Semi-transparent white container background */
  padding: 20px;
  border-radius: 10px;
  box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.3); /* Shadow for container */
  margin: 20px auto;
  max-width: 800px;
}

/* Text styles */
h1, h2, h3 {
  color: #ff69b4; /* Darker pink headers */
}
/* Base styles */
body {
  background: linear-gradient(to bottom, #ffb6c1, #ffc0cb); /* Light pink sunset gradient background */
  font-family: 'Cursive', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  color: #000; /* Black text color */
  margin: 0;
  padding: 0;
}

/* Header styles */
.header {
  background-color: #ffcccc; /* Light pink header background */
  color: #000; /* Black text color for header */
  padding: 20px;
  text-align: center;
  box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.3); /* Shadow for header */
}

/* Button styles */
.button {
  background-color: #ff69b4; /* Darker pink button background */
  color: #fff; /* White text color for buttons */
  padding: 10px 20px;
  border: none;
  border-radius: 5px;
  cursor: pointer;
  font-weight: bold;
  transition: background-color 0.3s; /* Smooth hover effect */
}

.button:hover {
  background-color: #ff1b1b; /* Darker pink on hover */
}

/* Links */
a {
  color: #ff69b4; /* Darker pink links */
  text-decoration: none;
  font-weight: bold;
  transition: color 0.3s; /* Smooth color change on hover */
}

a:hover {
  color: #ff1b1b; /* Darker pink on hover */
}

/* Sunset Image */
.sunset {
  max-width: 100%;
  height: auto;
  display: block;
  margin: 20px auto;
  box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.3); /* Shadow for image */
  border-radius: 10px;
}

/* Content container */
.container {
  background-color: rgba(255, 255, 255, 0.9); /* Semi-transparent white container background */
  padding: 20px;
  border-radius: 10px;
  box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.3); /* Shadow for container */
  margin: 20px auto;
  max-width: 800px;
}

/* Text styles */
h1, h2, h3 {
  color: #ff69b4; /* Darker pink headers */
  font-family: 'Cursive', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}

p {
  line-height: 1.6;
  color: #000; /* Black text color for paragraphs */
  font-family: 'Cursive', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}

/* Highlighted text */
.highlight {
  background-color: #fff; /* White background for highlight */
  color: #000; /* Black text color for highlighted text */
  padding: 3px 5px;
  border-radius: 3px;
}

/* Footer styles */
.footer {
  background-color: #ffcccc; /* Light pink footer background */
  color: #000; /* Black text color for footer */
  padding: 10px 0;
  text-align: center;
}
/* Base styles */
body {
  background: linear-gradient(to bottom, #ffb6c1, #ffc0cb); /* Light pink sunset gradient background */
  font-family: 'Your-Font-Here', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  color: #000; /* Black text color */
  margin: 0;
  padding: 0;
}

/* Header styles */
.header {
  background-color: #ffcccc; /* Light pink header background */
  color: #000; /* Black text color for header */
  padding: 20px;
  text-align: center;
  box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.3); /* Shadow for header */
}

/* Button styles */
.button {
  background-color: #ff69b4; /* Darker pink button background */
  color: #fff; /* White text color for buttons */
  padding: 10px 20px;
  border: none;
  border-radius: 5px;
  cursor: pointer;
  font-weight: bold;
  transition: background-color 0.3s; /* Smooth hover effect */
}

.button:hover {
  background-color: #ff1b1b; /* Darker pink on hover */
}

/* Links */
a {
  color: #ff69b4; /* Darker pink links */
  text-decoration: none;
  font-weight: bold;
  transition: color 0.3s; /* Smooth color change on hover */
}

a:hover {
  color: #ff1b1b; /* Darker pink on hover */
}

/* Sunset Image */
.sunset {
  max-width: 100%;
  height: auto;
  display: block;
  margin: 20px auto;
  box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.3); /* Shadow for image */
  border-radius: 10px;
}

/* Content container */
.container {
  background-color: rgba(255, 255, 255, 0.9); /* Semi-transparent white container background */
  padding: 20px;
  border-radius: 10px;
  box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.3); /* Shadow for container */
  margin: 20px auto;
  max-width: 800px;
}

/* Text styles */
h1, h2, h3 {
  color: #ff69b4; /* Darker pink headers */
  font-family: 'Your-Font-Here', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}

p {
  line-height: 1.6;
  color: #fff; /* White text color for paragraphs */
  font-family: 'Your-Font-Here', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  background-color: #ff69b4; /* Pink background for highlighted text */
  padding: 3px 5px;
  border-radius: 3px;
}

/* Footer styles */
.footer {
  background-color: #ffcccc; /* Light pink footer background */
  color: #000; /* Black text color for footer */
  padding: 10px 0;
  text-align: center;
}
